import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: 'Lexend, Open Sans, sans-serif',
    body: 'Lexend, Raleway, sans-serif',
  },
})

export default theme